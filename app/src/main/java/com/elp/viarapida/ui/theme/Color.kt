package com.elp.viarapida.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales
val Primary = Color(0xFFFF6B35)        // Naranja vibrante
val Secondary = Color(0xFF004E89)      // Azul profesional
val Tertiary = Color(0xFFFFB627)       // Amarillo dorado

// Colores de fondo
val Background = Color(0xFFF7F7F7)     // Gris muy claro
val Surface = Color(0xFFFFFFFF)        // Blanco

// Colores de texto
val OnPrimary = Color(0xFFFFFFFF)      // Texto sobre Primary
val OnSecondary = Color(0xFFFFFFFF)    // Texto sobre Secondary
val OnBackground = Color(0xFF1C1B1F)   // Texto sobre Background
val OnSurface = Color(0xFF1C1B1F)      // Texto sobre Surface

// Colores de estado
val Error = Color(0xFFD32F2F)          // Rojo para errores
val Success = Color(0xFF388E3C)        // Verde para éxito
val Warning = Color(0xFFF57C00)        // Naranja para advertencias
val Info = Color(0xFF1976D2)           // Azul para información

// Colores adicionales
val GrayLight = Color(0xFFE0E0E0)      // Gris claro para bordes
val GrayMedium = Color(0xFF9E9E9E)     // Gris medio para texto secundario
val GrayDark = Color(0xFF424242)       // Gris oscuro

// Colores para modo oscuro (opcional)
val PrimaryDark = Color(0xFFFF8A5B)
val BackgroundDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1E1E1E)